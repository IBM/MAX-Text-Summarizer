language: python
python:
  - 3.6

services:
  - docker

install:
  - docker build -t max-text-summarizer .
  - docker run -it --rm -d -p 5000:5000 max-text-summarizer
  - pip install -r requirements-test.txt

before_script:
  - sleep 30

script:
  - pytest tests/test.py
